{% import "_includes/forms" as forms %}

{{ 
    forms.checkboxField({
        label: 'This field is required'|t('one-plugin-media'),
        id: 'mandatory-toggle',
        name: 'mandatory',
        class: 'use-single-folder-cb',
        value: 1,
        checked: field.mandatory
    }) 
}}

{{ forms.checkboxSelectField({
    label: "Allowed Content Types"|t('app'),
    instructions: "Set the allowed content types for this field"|t('app'),
    id: 'allowedContents',
    name: 'allowedContents',
    options: availableContents,
    values: field.allowedContents,
    showAllOption: true,
}) }}

{{ forms.checkboxSelectField({
    label: "Sources"|t('one-plugin-media'),
    instructions: "Which sources do you want to select image from?",
    id: 'oneplugin-asset-sources',
    name: 'allowedSources',
    options: availableSources,
    values: field.allowedSources,
    showAllOption: true,
}) }}
